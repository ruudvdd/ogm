parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        phpcs:
            standard: "PSR2"
            whitelist_patterns:
              - /^src\/(.*)/
        phpunit:
            config_file: .
